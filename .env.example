HOST_IP=192.168.68.135
# HOST_IP=100.86.254.69

# camera (default) = backend polls the inference server /status (presenceService.js).
# sensor = ESP32 distance sensor drives presence over MQTT. That hardware is
# retired; kept selectable only in case it's ever reinstalled.
PRESENCE_SOURCE=camera
# PRESENCE_STATUS_URL=
